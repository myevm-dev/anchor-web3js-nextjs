[programs.localnet]
pump_vaults = "YOUR_PROGRAM_ID_HERE"   # replace after `anchor keys set`

[registry]
url = "https://anchor.projectserum.com"

[provider]
cluster = "Localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "node tests/pump_vaults.js"
